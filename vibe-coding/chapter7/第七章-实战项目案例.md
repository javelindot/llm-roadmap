# 第七章：实战项目案例

## 7.1 实战项目概述

### 本章内容

本章通过 6 个实战案例，展示如何使用 AI 编程工具完成真实项目：

```
┌─────────────────────────────────────────────────────────────┐
│                      实战项目列表                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   7.1  Web 应用: Next.js 博客系统                          │
│   ──────────────────────────────────────                    │
│   从零开始，包含前端、后端、数据库                            │
│                                                             │
│   7.2  API 服务: RESTful + GraphQL                         │
│   ──────────────────────────────────────                    │
│   类型安全的 API 开发，包含认证和验证                          │
│                                                             │
│   7.3  数据分析: Pandas + 可视化                            │
│   ──────────────────────────────────────                    │
│   数据处理脚本，自动化报告生成                                 │
│                                                             │
│   7.4  CLI 工具: 命令行工具开发                             │
│   ──────────────────────────────────────                    │
│   从想法到 npm 包发布的完整流程                               │
│                                                             │
│   7.5  Chrome 扩展: 效率工具开发                            │
│   ──────────────────────────────────────                    │
│   Manifest V3 插件开发                                        │
│                                                             │
│   7.6  全栈 SaaS: 完整项目                                  │
│   ──────────────────────────────────────                    │
│   Next.js + Prisma + Vercel + Stripe                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 7.2 案例一：Next.js 博客系统

### 项目目标

```
┌─────────────────────────────────────────────────────────────┐
│                   博客系统功能需求                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   前端:                                                     │
│   ├─ 首页文章列表                                           │
│   ├─ 文章详情页                                             │
│   ├─ 分类/标签筛选                                         │
│   ├─ 评论功能                                               │
│   └─ Markdown 编辑器                                        │
│                                                             │
│   后端:                                                     │
│   ├─ 用户认证 (注册/登录/JWT)                              │
│   ├─ 文章 CRUD                                             │
│   ├─ 评论管理                                              │
│   └─ 文件上传                                              │
│                                                             │
│   数据库:                                                   │
│   ├─ Users 表                                              │
│   ├─ Posts 表                                             │
│   ├─ Comments 表                                           │
│   └─ Tags 表                                               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 开发流程

#### Step 1: 项目初始化

```bash
# 使用 Cursor Composer 初始化项目
> 创建一个 Next.js 14 博客项目，包含:
  - TypeScript
  - Tailwind CSS
  - App Router
  - Prisma ORM
  - NextAuth 认证
```

#### Step 2: 数据库设计

```bash
# 使用 Cursor Composer 生成 Prisma Schema
> 为博客系统创建 Prisma Schema，包含:
  - User: id, email, name, password, role
  - Post: id, title, slug, content, published, authorId
  - Comment: id, content, authorId, postId
  - Tag: id, name, posts (多对多)
```

生成结果:

```prisma
// prisma/schema.prisma

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String    @id @default(cuid())
  email     String    @unique
  name      String?
  password  String
  role      Role      @default(USER)
  posts     Post[]
  comments  Comment[]
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model Post {
  id        String    @id @default(cuid())
  title     String
  slug      String    @unique
  content   String    @db.Text
  published Boolean   @default(false)
  author    User      @relation(fields: [authorId], references: [id])
  authorId  String
  comments  Comment[]
  tags      Tag[]     @relation("PostTags")
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
}

model Comment {
  id        String   @id @default(cuid())
  content   String
  author    User     @relation(fields: [authorId], references: [id])
  authorId  String
  post      Post     @relation(fields: [postId], references: [id])
  postId    String
  createdAt DateTime @default(now())
}

model Tag {
  id    String @id @default(cuid())
  name  String @unique
  posts Post[] @relation("PostTags")
}

enum Role {
  USER
  ADMIN
}
```

#### Step 3: API 路由开发

```bash
# 使用 Cursor Composer 生成 API
> 为博客系统创建 API 路由:
  - /api/posts (GET: 列表, POST: 创建)
  - /api/posts/[slug] (GET: 详情, PUT: 更新, DELETE: 删除)
  - /api/posts/[slug]/comments (GET: 评论, POST: 添加)
  - /api/auth/[...nextauth] (认证)
```

#### Step 4: 前端组件开发

```bash
# 使用 Cursor Composer 生成组件
> 创建以下组件:
  - PostCard: 文章卡片
  - PostEditor: Markdown 编辑器
  - CommentSection: 评论区域
  - TagFilter: 标签筛选
```

#### Step 5: 页面开发

```bash
# 使用 Cursor Composer 生成页面
> 创建以下页面:
  - / (首页，文章列表)
  - /posts/[slug] (文章详情)
  - /admin (管理后台)
  - /login (登录)
```

### 效率对比

| 任务 | 传统方式 | AI 辅助 | 提升 |
|------|----------|---------|------|
| 项目初始化 | 30 分钟 | 5 分钟 | 6x |
| Schema 设计 | 20 分钟 | 3 分钟 | 7x |
| API 开发 | 2 小时 | 20 分钟 | 6x |
| 组件开发 | 1 小时 | 15 分钟 | 4x |
| **总计** | **~4 小时** | **~45 分钟** | **~5x** |

---

## 7.3 案例二：RESTful API 服务

### 项目目标

```
┌─────────────────────────────────────────────────────────────┐
│                   电商 API 功能需求                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   用户模块:                                                 │
│   ├─ 注册/登录/JWT                                          │
│   ├─ 个人信息管理                                           │
│   └─ 地址管理                                               │
│                                                             │
│   商品模块:                                                 │
│   ├─ 商品 CRUD                                             │
│   ├─ 分类管理                                              │
│   └─ 搜索和筛选                                             │
│                                                             │
│   订单模块:                                                 │
│   ├─ 创建订单                                              │
│   ├─ 订单状态管理                                          │
│   └─ 支付集成                                              │
│                                                             │
│   技术要求:                                                  │
│   ├─ TypeScript + Express                                 │
│   ├─ Zod 输入验证                                          │
│   ├─ 统一错误处理                                          │
│   └─ 完整测试覆盖                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 开发流程

#### Step 1: 项目结构

```bash
> 创建一个 Node.js API 项目，结构如下:
  - src/
    - routes/      (路由)
    - controllers/  (控制器)
    - models/       (数据模型)
    - middleware/   (中间件)
    - utils/        (工具函数)
    - types/        (类型定义)
  - tests/          (测试)
  - 使用 TypeScript
```

#### Step 2: 核心类型和 Schema

```bash
> 创建以下类型定义和验证 Schema:
  - User, Product, Order 类型
  - Zod 验证 Schema
  - API Response 类型
```

生成结果示例:

```typescript
// src/types/index.ts

// 用户类型
export interface User {
  id: string;
  email: string;
  name: string;
  role: 'customer' | 'admin';
  createdAt: Date;
}

// 产品类型
export interface Product {
  id: string;
  name: string;
  description: string;
  price: number;
  stock: number;
  category: string;
  images: string[];
  createdAt: Date;
}

// 订单类型
export interface Order {
  id: string;
  userId: string;
  items: OrderItem[];
  total: number;
  status: 'pending' | 'paid' | 'shipped' | 'delivered';
  createdAt: Date;
}

// API 响应类型
export interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: string;
  message?: string;
}
```

#### Step 3: 中间件开发

```bash
> 创建以下中间件:
  - authMiddleware: JWT 验证
  - errorHandler: 统一错误处理
  - validateRequest: Zod 验证
  - rateLimiter: 限流
```

#### Step 4: 路由和控制器

```bash
# 分模块生成
> 为用户模块创建:
  - routes/userRoutes.ts
  - controllers/userController.ts
  - 确保符合 RESTful 规范
```

### API 设计示例

```typescript
// src/routes/productRoutes.ts

import { Router } from 'express';
import { ProductController } from '../controllers/productController';
import { authenticate, authorize } from '../middleware/auth';
import { validateRequest } from '../middleware/validate';
import { z } from 'zod';

const router = Router();

// 查询参数 Schema
const querySchema = z.object({
  page: z.coerce.number().min(1).default(1),
  limit: z.coerce.number().min(1).max(100).default(20),
  category: z.string().optional(),
  search: z.string().optional(),
  minPrice: z.coerce.number().optional(),
  maxPrice: z.coerce.number().optional(),
});

// Routes
router.get('/', validateRequest({ query: querySchema }), ProductController.list);
router.get('/:id', ProductController.get);
router.post('/', authenticate, authorize('admin'), ProductController.create);
router.put('/:id', authenticate, authorize('admin'), ProductController.update);
router.delete('/:id', authenticate, authorize('admin'), ProductController.delete);

export default router;
```

---

## 7.4 案例三：数据分析项目

### 项目目标

```
┌─────────────────────────────────────────────────────────────┐
│                   数据分析自动化需求                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   数据处理:                                                 │
│   ├─ 读取 CSV/Excel 文件                                   │
│   ├─ 数据清洗和转换                                         │
│   ├─ 多数据源合并                                           │
│   └─ 异常值检测                                             │
│                                                             │
│   分析功能:                                                 │
│   ├─ 描述性统计                                            │
│   ├─ 趋势分析                                              │
│   ├─ 分组聚合                                              │
│   └─ 相关性分析                                            │
│                                                             │
│   可视化:                                                   │
│   ├─ Matplotlib 图表                                       │
│   ├─ Plotly 交互图表                                       │
│   └─ 自动生成报告                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 开发流程

#### Step 1: 数据读取和处理

```bash
> 创建一个数据分析脚本，实现:
  - 读取 sales_data.csv
  - 数据清洗（缺失值、异常值）
  - 数据转换和标准化
```

生成结果:

```python
import pandas as pd
import numpy as np
from pathlib import Path

class SalesDataProcessor:
    """销售数据分析处理器"""

    def __init__(self, file_path: str):
        self.df = pd.read_csv(file_path)
        self.original_shape = self.df.shape

    def clean(self) -> 'SalesDataProcessor':
        """数据清洗"""
        # 删除重复行
        self.df.drop_duplicates(inplace=True)

        # 处理缺失值
        self.df['sales'].fillna(self.df['sales'].median(), inplace=True)
        self.df['region'].fillna('Unknown', inplace=True)

        # 转换日期格式
        self.df['date'] = pd.to_datetime(self.df['date'])

        # 处理异常值（使用 IQR 方法）
        Q1 = self.df['sales'].quantile(0.25)
        Q3 = self.df['sales'].quantile(0.75)
        IQR = Q3 - Q1
        lower_bound = Q1 - 1.5 * IQR
        upper_bound = Q3 + 1.5 * IQR
        self.df = self.df[
            (self.df['sales'] >= lower_bound) &
            (self.df['sales'] <= upper_bound)
        ]

        return self

    def transform(self) -> 'SalesDataProcessor':
        """数据转换"""
        # 添加派生列
        self.df['year'] = self.df['date'].dt.year
        self.df['month'] = self.df['date'].dt.month
        self.df['quarter'] = self.df['date'].dt.quarter
        self.df['day_of_week'] = self.df['date'].dt.day_name()

        return self

    def get_summary(self) -> dict:
        """获取数据摘要"""
        return {
            'original_rows': self.original_shape[0],
            'cleaned_rows': len(self.df),
            'columns': list(self.df.columns),
            'date_range': f"{self.df['date'].min()} to {self.df['date'].max()}",
            'total_sales': self.df['sales'].sum(),
            'avg_sales': self.df['sales'].mean()
        }
```

#### Step 2: 分析和可视化

```bash
> 添加分析和可视化功能:
  - 按月/季度销售趋势
  - 按地区分组统计
  - 产品类别分析
  - 生成 Matplotlib 图表
```

#### Step 3: 报告生成

```bash
> 添加自动化报告功能:
  - 生成 PDF 报告
  - 包含关键指标
  - 包含可视化图表
```

---

## 7.5 案例四：CLI 工具开发

### 项目目标

```
┌─────────────────────────────────────────────────────────────┐
│                   CLI 工具功能需求                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   功能:                                                     │
│   ├─ 文件批量重命名                                         │
│   ├─ 支持正则表达式                                        │
│   ├─ 预览模式（不实际操作）                                 │
│   ├─ 撤销功能                                              │
│   └─ 批量操作日志                                          │
│                                                             │
│   技术:                                                     │
│   ├─ Node.js + TypeScript                                 │
│   ├─ Commander.js 命令行框架                               │
│   ├─ Chalk 彩色输出                                        │
│   └─ 支持全局安装                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 开发流程

#### Step 1: 项目初始化

```bash
> 创建一个 CLI 工具项目:
  - 项目名: file-renamer
  - TypeScript
  - Commander.js
  - 支持 ESM
```

#### Step 2: 命令行界面

```bash
> 创建 CLI 界面，支持以下命令:
  - file-renamer rename <pattern> <replacement>
  - file-renamer preview <pattern> <replacement>
  - file-renamer undo
  - file-renamer log
```

生成结果:

```typescript
#!/usr/bin/env node

import { Command } from 'commander';
import { rename } from './commands/rename';
import { preview } from './commands/preview';
import { undo } from './commands/undo';
import { log } from './commands/log';

const program = new Command();

program
  .name('file-renamer')
  .description('A powerful file batch renaming tool')
  .version('1.0.0');

// rename 命令
program
  .command('rename')
  .description('Rename files matching pattern')
  .argument('<pattern>', 'Regex pattern to match files')
  .argument('<replacement>', 'Replacement string')
  .option('-d, --dir <path>', 'Target directory', '.')
  .option('-e, --ext <extension>', 'File extension filter')
  .option('-r, --recursive', 'Recursive search', false)
  .action(rename);

// preview 命令
program
  .command('preview')
  .description('Preview rename operations without executing')
  .argument('<pattern>', 'Regex pattern to match files')
  .argument('<replacement>', 'Replacement string')
  .option('-d, --dir <path>', 'Target directory', '.')
  .action(preview);

// undo 命令
program
  .command('undo')
  .description('Undo the last rename operation')
  .option('--all', 'Undo all operations', false)
  .action(undo);

// log 命令
program
  .command('log')
  .description('Show rename history')
  .option('--json', 'Output as JSON', false)
  .action(log);

program.parse();
```

#### Step 3: 核心功能实现

```bash
> 实现核心功能:
  - 文件扫描
  - 正则替换
  - 操作日志
  - 撤销机制
```

---

## 7.6 案例五：Chrome 扩展开发

### 项目目标

```
┌─────────────────────────────────────────────────────────────┐
│                   Chrome 扩展需求                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   功能:                                                     │
│   ├─ 网页内容高亮和批注                                     │
│   ├─ 笔记管理                                              │
│   ├─ 导出到 Markdown                                       │
│   └─ 本地存储                                              │
│                                                             │
│   技术:                                                     │
│   ├─ Manifest V3                                           │
│   ├─ React + TypeScript Popup                             │
│   ├─ Content Script                                       │
│   └─ Chrome Storage API                                   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 开发流程

#### Step 1: 项目结构

```bash
> 创建 Chrome 扩展项目:
  - Manifest V3
  - React + TypeScript
  - 组件: Popup, Options, Content Script
```

#### Step 2: Manifest 配置

```bash
> 创建 manifest.json:
  - permissions
  - content_scripts
  - action (popup)
  - storage
```

生成结果:

```json
{
  "manifest_version": 3,
  "name": "Web Highlighter",
  "version": "1.0.0",
  "description": "Highlight and annotate web pages",
  "permissions": ["storage", "activeTab"],
  "host_permissions": ["<all_urls>"],
  "action": {
    "default_popup": "popup.html",
    "default_icon": "icons/icon16.png"
  },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content/content.js"],
      "css": ["content/content.css"]
    }
  ],
  "icons": {
    "16": "icons/icon16.png",
    "48": "icons/icon48.png",
    "128": "icons/icon128.png"
  }
}
```

---

## 7.7 案例六：全栈 SaaS 项目

### 项目概述

```
┌─────────────────────────────────────────────────────────────┐
│                   SaaS 项目: AI 笔记应用                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   功能:                                                     │
│   ├─ 用户认证 (Google/GitHub OAuth)                        │
│   ├─ 笔记 CRUD                                             │
│   ├─ AI 辅助写作 (Claude 集成)                             │
│   ├─ 分享和协作                                            │
│   └─ 订阅支付 (Stripe)                                     │
│                                                             │
│   技术栈:                                                   │
│   ├─ 前端: Next.js 14 + Tailwind + React                   │
│   ├─ 后端: Next.js API Routes + Prisma                    │
│   ├─ 数据库: PostgreSQL (Neon)                             │
│   ├─ 认证: NextAuth.js                                     │
│   ├─ 支付: Stripe                                          │
│   ├─ AI: Anthropic Claude API                             │
│   └─ 部署: Vercel                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 开发策略

```bash
# 第一阶段: 基础框架 (1天)
> 初始化 Next.js 项目
> 配置 Prisma + PostgreSQL
> 设置 NextAuth 认证
> 创建基础 UI 组件

# 第二阶段: 核心功能 (2天)
> 笔记 CRUD API
> 笔记编辑器
> 笔记列表和搜索

# 第三阶段: AI 功能 (1天)
> Claude API 集成
> AI 写作辅助
> AI 摘要生成

# 第四阶段: 支付和部署 (1天)
> Stripe 订阅集成
> 前端订阅 UI
> Vercel 部署
```

### 效率提升

| 阶段 | 传统方式 | AI 辅助 | 节省 |
|------|----------|---------|------|
| 基础框架 | 2-3 天 | 3-4 小时 | 80% |
| 核心功能 | 3-5 天 | 1 天 | 75% |
| AI 功能 | 2-3 天 | 4 小时 | 80% |
| 支付集成 | 1-2 天 | 2 小时 | 85% |
| **总计** | **9-15 天** | **2-3 天** | **~80%** |

---

## 7.8 总结

### 关键经验

1. **分步骤执行**：复杂项目拆分为多个步骤
2. **复用模板**：建立自己的 Prompt 模板库
3. **持续验证**：每步生成后验证再继续
4. **人工审核**：AI 生成仍需人工审查

### 工具选择建议

| 项目类型 | 推荐工具 | 理由 |
|----------|----------|------|
| Web 应用 | Cursor | 快速多文件生成 |
| API 服务 | Cursor | 类型安全、Schema 生成 |
| 数据分析 | Claude Code | 复杂逻辑处理 |
| CLI 工具 | Cursor | 命令行代码生成 |
| Chrome 扩展 | Cursor | 完整项目结构 |
| SaaS | Cursor + Claude | 各取所长 |

### 思考题

1. 你在项目中遇到过哪些 AI 生成代码的问题？
2. 如何平衡 AI 生成效率和代码质量？
3. 你会如何调整这些案例以适应你的项目？

### 下章预告

下一章将介绍 **个人开发最佳实践**，包括：
- 工作流设计
- Cursor 和 Claude 工作流
- 调试策略
- 版本控制
- 效率技巧
